# Skip installing optional dependencies to avoid platform-specific native rollup packages
# This helps Vercel/npm avoid trying to fetch @rollup/rollup-*-* optional binaries that sometimes fail
optional=false
